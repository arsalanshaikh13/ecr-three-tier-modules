name: Build, Push, and Deploy to ECS

on:
  push:
    branches:
      - multi-env
      - multi-env-actions
    paths:
      - "lirw-three-tier/frontend/**"
      - "lirw-three-tier/backend/**"
      - "lirw-three-tier/probe/**"
  workflow_dispatch:
    inputs:
      action_type:
        description: "Run a normal deploy flow or trigger a manual rollback"
        type: choice
        required: true
        default: deploy
        options:
          - deploy
          - rollback
      target_environment:
        description: "Target environment to deploy"
        type: choice
        required: true
        default: dev
        options:
          - dev
          - prod
          - all
      build_frontend:
        description: "Build & Deploy Frontend?"
        type: boolean
        required: true
        default: true
      get_frontend:
        description: "Get Frontend image from ECR?"
        type: boolean
        required: true
        default: false
      build_backend:
        description: "Build & Deploy Backend?"
        type: boolean
        required: true
        default: true
      get_backend:
        description: "Get Backend image from ECR?"
        type: boolean
        required: true
        default: false
      run_seeding:
        description: "Build Seeder & Seed Database?"
        type: boolean
        required: true
        default: false
      default_launch_type:
        description: "Default launch type for jobs that support runtime overrides."
        type: choice
        required: true
        default: EC2
        options:
          - EC2
          - FARGATE
      default_network_mode:
        description: "Default network mode for jobs that support runtime overrides."
        type: choice
        required: true
        default: non-awsvpc
        options:
          - non-awsvpc
          - awsvpc
      release_version:
        description: "Optional immutable release version. Required for prod-targeting guarded runs unless break-glass is used."
        type: string
        required: false
      change_ticket:
        description: "Optional change ticket or approval reference. Required for prod-targeting guarded runs unless break-glass is used."
        type: string
        required: false
      break_glass_reason:
        description: "Optional emergency override reason for guarded prod operations."
        type: string
        required: false
      enable_security_scan:
        description: "Run the pluggable image security gate during deploy preparation?"
        type: boolean
        required: true
        default: false
      enable_sbom:
        description: "Generate an SBOM for the resolved deploy image?"
        type: boolean
        required: true
        default: false
      enable_telemetry_gate:
        description: "Evaluate CloudWatch release-health alarms after probe succeeds?"
        type: boolean
        required: true
        default: false
      observation_window_minutes:
        description: "Minutes to observe release health before reading CloudWatch alarms."
        type: string
        required: true
        default: "5"
      vulnerability_threshold:
        description: "Severity threshold passed into the image security gate when enabled."
        type: choice
        required: true
        default: CRITICAL
        options:
          - CRITICAL
          - HIGH,CRITICAL
      rollback_component:
        description: "Component to roll back when action_type is rollback"
        type: choice
        required: false
        default: backend
        options:
          - frontend
          - backend
      rollback_task_definition_arn:
        description: "Optional explicit task definition ARN for manual rollback"
        type: string
        required: false
      rollback_manifest_s3_key:
        description: "Optional S3 manifest key whose task_definition_arn should be rolled back"
        type: string
        required: false

permissions:
  id-token: write
  contents: read

jobs:
  predeploy-guardrails:
    name: Predeploy Guardrails
    # Keep governance in a reusable workflow so release policy can evolve independently from
    # deploy mechanics. This lets us bolt on stronger prod controls without hardwiring them
    # across every job in deploy.yml.
    uses: ./.github/workflows/reusable-predeploy-guardrails.yml
    with:
      target_environment: ${{ github.event_name == 'workflow_dispatch' && inputs.target_environment || 'dev' }}
      action_type: ${{ github.event_name == 'workflow_dispatch' && inputs.action_type || 'deploy' }}
      release_version: ${{ github.event_name == 'workflow_dispatch' && inputs.release_version || '' }}
      change_ticket: ${{ github.event_name == 'workflow_dispatch' && inputs.change_ticket || '' }}
      break_glass_reason: ${{ github.event_name == 'workflow_dispatch' && inputs.break_glass_reason || '' }}
      freeze_windows_utc: ${{ vars.PROD_FREEZE_WINDOWS_UTC || '' }}

  # ---------------------------------------------------------
  # 1. DETECT CHANGES (Path Filtering)
  # ---------------------------------------------------------
  detect-changes:
    name: Detect Changed Paths & Setup Matrix
    needs: predeploy-guardrails
    runs-on: ubuntu-latest
    outputs:
      # Output includes component+environment object matrix for build-and-deploy job.
      matrix: ${{ steps.set-matrix.outputs.matrix }}
      # Output environment list for seed job matrix.
      env_matrix: ${{ steps.set-matrix.outputs.env_matrix }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          # Push-based change detection should compare the pushed revision against the
          # commit that immediately preceded it, not against main. Fetching a little
          # extra history makes that comparison available inside the runner.
          fetch-depth: 2

      - name: Resolve safe diff base for push events
        id: diff-base
        shell: bash
        run: |
          set -euo pipefail

          ZERO_SHA="0000000000000000000000000000000000000000"
          BEFORE_SHA="${{ github.event.before }}"

          # Normal pushes provide a real "before" SHA and should diff against that exact
          # previous revision. This keeps path filtering scoped to the current push instead
          # of drifting toward a long-lived branch vs main comparison.
          if [ "$BEFORE_SHA" != "$ZERO_SHA" ]; then
            echo "base=$BEFORE_SHA" >> "$GITHUB_OUTPUT"
            echo "use_fallback_scan=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          # Some first-push or branch-creation events surface an all-zero SHA. In that case,
          # prefer the local parent commit when available instead of falling back to main,
          # because main would reintroduce the "branch always looks changed" problem.
          if git rev-parse HEAD^ >/dev/null 2>&1; then
            echo "base=$(git rev-parse HEAD^)" >> "$GITHUB_OUTPUT"
            echo "use_fallback_scan=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          # If even a parent commit is unavailable, treat monitored app areas as changed.
          # This intentionally biases toward running CI/CD once on the first push rather than
          # silently skipping deployable code because no safe diff base exists.
          echo "base=" >> "$GITHUB_OUTPUT"
          echo "use_fallback_scan=true" >> "$GITHUB_OUTPUT"

      - uses: dorny/paths-filter@v3
        id: filter
        if: ${{ steps.diff-base.outputs.use_fallback_scan != 'true' }}
        with:
          # On push events, anchor change detection to the exact before/after SHAs from
          # the webhook payload so feature branches do not look "changed forever" just
          # because they differ from main. Manual dispatch remains the path for workflow testing.
          base: ${{ steps.diff-base.outputs.base }}
          ref: ${{ github.sha }}
          filters: |
            frontend:
              - 'lirw-three-tier/frontend/**'
            backend:
              - 'lirw-three-tier/backend/**'
            probe:
              - 'lirw-three-tier/probe/**'

      - name: Fallback path flags for zero-SHA first push
        id: filter-fallback
        if: ${{ steps.diff-base.outputs.use_fallback_scan == 'true' }}
        shell: bash
        run: |
          # This fallback is intentionally simple: if GitHub did not give us a usable diff
          # base and the local clone has no parent commit to compare against, assume the
          # monitored app surfaces changed for this first push.
          echo "frontend=true" >> "$GITHUB_OUTPUT"
          echo "backend=true" >> "$GITHUB_OUTPUT"
          echo "probe=true" >> "$GITHUB_OUTPUT"

      - name: Calculate Dynamic Matrix
        id: set-matrix
        run: |
          COMPONENTS=()
          TARGET_ENVS=()

          # 1. PUSH EVENT: Only build based on path filters
          if [ "${{ github.event_name }}" == "push" ]; then
            FRONTEND_CHANGED="${{ steps.filter.outputs.frontend || steps.filter-fallback.outputs.frontend }}"
            BACKEND_CHANGED="${{ steps.filter.outputs.backend || steps.filter-fallback.outputs.backend }}"

            if [ "$FRONTEND_CHANGED" == "true" ]; then COMPONENTS+=("frontend"); fi
            if [ "$BACKEND_CHANGED" == "true" ]; then COMPONENTS+=("backend"); fi
            # Probe is built in its own post-deploy job, so we only need app components here.
            # For push events we deploy dev only by default to keep production gated.
            TARGET_ENVS=("dev")

          # 2. MANUAL TRIGGER: Include if EITHER build or get is true
          elif [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
            # Manual rollback is a separate job path, so deployment matrices should stay empty.
            if [ "${{ inputs.action_type }}" == "rollback" ]; then
              echo "matrix=[]" >> $GITHUB_OUTPUT
              echo "env_matrix=[]" >> $GITHUB_OUTPUT
              exit 0
            fi

            # Check Frontend
            if [[ "${{ inputs.build_frontend }}" == "true" || "${{ inputs.get_frontend }}" == "true" ]]; then
               COMPONENTS+=("frontend")
            fi
            # Check Backend
            if [[ "${{ inputs.build_backend }}" == "true" || "${{ inputs.get_backend }}" == "true" ]]; then
               COMPONENTS+=("backend")
            fi

            # Manual trigger can target one environment or both in parallel.
            if [ "${{ inputs.target_environment }}" == "all" ]; then
              TARGET_ENVS=("dev" "prod")
            else
              TARGET_ENVS=("${{ inputs.target_environment }}")
            fi
          fi

          # Build object matrix: [{ "component":"frontend", "environment":"dev" }, ...]
          if [ ${#COMPONENTS[@]} -eq 0 ] || [ ${#TARGET_ENVS[@]} -eq 0 ]; then
            MATRIX_JSON="[]"
          else
            MATRIX_JSON=$(jq -cn \
              --argjson comps "$(printf '%s\n' "${COMPONENTS[@]}" | jq -R . | jq -s .)" \
              --argjson envs "$(printf '%s\n' "${TARGET_ENVS[@]}" | jq -R . | jq -s .)" \
              '[ $comps[] as $c | $envs[] as $e | {component: $c, environment: $e} ]')
          fi

          # Environment-only matrix for seeding job.
          if [ ${#TARGET_ENVS[@]} -eq 0 ]; then
            ENV_MATRIX_JSON="[]"
          else
            ENV_MATRIX_JSON=$(printf '%s\n' "${TARGET_ENVS[@]}" | jq -R . | jq -s -c .)
          fi

          echo "Calculated Matrix: $MATRIX_JSON"
          echo "Calculated Env Matrix: $ENV_MATRIX_JSON"
          echo "matrix=$MATRIX_JSON" >> $GITHUB_OUTPUT
          echo "env_matrix=$ENV_MATRIX_JSON" >> $GITHUB_OUTPUT
  # ---------------------------------------------------------
  # 2. BUILD & DEPLOY (Matrix for Frontend / Backend)
  # ---------------------------------------------------------
  build-and-deploy:
    name: Build & Deploy ${{ matrix.component }} (${{ matrix.environment }})
    needs: [predeploy-guardrails, detect-changes]
    # CRITICAL: If the array is empty, skip this job entirely to prevent matrix errors
    if: ${{ needs.detect-changes.outputs.matrix != '[]' }}
    strategy:
      fail-fast: false
      matrix:
        # This dynamically loads component+environment objects from detect-changes.
        include: ${{ fromJson(needs.detect-changes.outputs.matrix) }}
    # The caller keeps only orchestration concerns here: matrix expansion, deploy policy
    # selection, and dependency wiring. The cloud mechanics now live in a reusable workflow.
    uses: ./.github/workflows/reusable-deploy-service.yml
    with:
      environment: ${{ matrix.environment }}
      component: ${{ matrix.component }}
      project_name: ${{ vars.PROJECT_NAME }}
      release_version: ${{ needs.predeploy-guardrails.outputs.resolved_release_version }}
      enable_security_scan: ${{ github.event_name == 'workflow_dispatch' && inputs.enable_security_scan || false }}
      enable_sbom: ${{ github.event_name == 'workflow_dispatch' && inputs.enable_sbom || false }}
      vulnerability_threshold: ${{ github.event_name == 'workflow_dispatch' && inputs.vulnerability_threshold || 'CRITICAL' }}
      # Workflow-level defaults are explicit test controls now: callers can still override
      # specific jobs later, but the dispatch contract always speaks in concrete ECS terms.
      service_network_mode: ${{ (github.event_name == 'workflow_dispatch' && inputs.default_network_mode) || vars.SERVICE_NETWORK_MODE }}
      # Service networking is component-aware now so frontend and backend can evolve toward
      # separate private subnet groups without teaching the lower-level actions about tiers.
      subnet_tag_values: ${{ (matrix.component == 'frontend' && vars.FRONTEND_SERVICE_SUBNET_TAG_VALUES) || (matrix.component == 'backend' && vars.BACKEND_SERVICE_SUBNET_TAG_VALUES) || vars.SERVICE_SUBNET_TAG_VALUES || '' }}
      security_group_name: ${{ (matrix.component == 'frontend' && vars.FRONTEND_SERVICE_SECURITY_GROUP_NAME) || (matrix.component == 'backend' && vars.BACKEND_SERVICE_SECURITY_GROUP_NAME) || vars.SERVICE_SECURITY_GROUP_NAME || '' }}
      assign_public_ip: ${{ (matrix.component == 'frontend' && vars.FRONTEND_SERVICE_ASSIGN_PUBLIC_IP) || (matrix.component == 'backend' && vars.BACKEND_SERVICE_ASSIGN_PUBLIC_IP) || vars.SERVICE_ASSIGN_PUBLIC_IP || '' }}
      # Caller-owned image policy keeps the reusable deploy workflow honest: it executes the
      # requested strategy, while deploy.yml remains the single place where rollout intent lives.
      # Push events always build fresh images. Manual runs can choose per component whether
      # deploy should build a new image or reuse one that is already in ECR.
      build_image: ${{ github.event_name == 'push' || (matrix.component == 'frontend' && inputs.build_frontend) || (matrix.component == 'backend' && inputs.build_backend) }}
      fetch_image: ${{ github.event_name == 'workflow_dispatch' && ((matrix.component == 'frontend' && inputs.get_frontend) || (matrix.component == 'backend' && inputs.get_backend)) }}
    secrets: inherit

  notify-deploy-started:
    name: Notify Deploy Started ${{ matrix.component }} (${{ matrix.environment }})
    needs: [predeploy-guardrails, detect-changes]
    if: ${{ needs.detect-changes.outputs.matrix != '[]' && (github.event_name == 'workflow_dispatch' || matrix.environment == 'prod') }}
    strategy:
      fail-fast: false
      matrix:
        include: ${{ fromJson(needs.detect-changes.outputs.matrix) }}
    runs-on: ubuntu-latest
    steps:
      - name: Configure AWS credentials for notification publishing
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::${{ vars.ACCOUNT_ID }}:role/GitHubActions-ECS-DeployRole
          aws-region: ${{ matrix.environment == 'prod' && (vars.PROD_AWS_REGION || 'us-east-2') || (vars.DEV_AWS_REGION || 'us-east-1') }}

      - name: Publish deploy-started notification
        uses: ./.github/actions/send-release-notification
        with:
          topic_arn: ${{ matrix.environment == 'prod' && vars.PROD_RELEASE_NOTIFICATIONS_TOPIC_ARN || vars.DEV_RELEASE_NOTIFICATIONS_TOPIC_ARN || '' }}
          aws_region: ${{ matrix.environment == 'prod' && (vars.PROD_AWS_REGION || 'us-east-2') || (vars.DEV_AWS_REGION || 'us-east-1') }}
          notification_event: deploy_started
          project_name: ${{ vars.PROJECT_NAME }}
          environment: ${{ matrix.environment }}
          component: ${{ matrix.component }}
          status: in_progress
          release_version: ${{ needs.predeploy-guardrails.outputs.resolved_release_version }}
          change_ticket: ${{ github.event_name == 'workflow_dispatch' && inputs.change_ticket || '' }}
          workflow_url: ${{ format('{0}/{1}/actions/runs/{2}', github.server_url, github.repository, github.run_id) }}
          summary: ${{ github.event_name == 'workflow_dispatch' && 'Manual deploy request entered the reusable deploy/probe/telemetry workflow graph.' || 'Push-triggered deploy entered the reusable deploy/probe/telemetry workflow graph.' }}

  # ---------------------------------------------------------
  # 3. POST-DEPLOY PROBE (Runs once per environment)
  # ---------------------------------------------------------
  probe-app:
    name: Probe App (${{ matrix.environment }})
    needs: [predeploy-guardrails, detect-changes, build-and-deploy]
    if: ${{ needs.detect-changes.outputs.env_matrix != '[]' || needs.detect-changes.outputs.matrix != '[]' }}
    strategy:
      fail-fast: false
      matrix:
        environment: ${{ fromJson(needs.detect-changes.outputs.env_matrix) }}
    # Probe is now a reusable orchestration unit because it combines task registration,
    # awsvpc execution, log surfacing, pointer updates, and manifest persistence.
    uses: ./.github/workflows/reusable-probe-environment.yml
    with:
      environment: ${{ matrix.environment }}
      project_name: ${{ vars.PROJECT_NAME }}
      # Probe image policy also stays caller-owned. Normal deploy validation uses a
      # fetch-first strategy so the first probe can self-seed ECR and later probes
      # reuse that image instead of rebuilding it every time.
      build_image: true
      fetch_image: false
      # EC2 based probes take too long to stop
      # Keep all probe-style jobs on the faster-stop Fargate/awsvpc path instead of
      # inheriting broader workflow defaults that are better suited to long-lived services.
      probe_launch_type: FARGATE
      probe_network_mode: awsvpc
    secrets: inherit

  # ---------------------------------------------------------
  # 4. POST-PROBE TELEMETRY GATE
  # ---------------------------------------------------------
  evaluate-release-health:
    name: Evaluate Release Health ${{ matrix.component }} (${{ matrix.environment }})
    needs: [predeploy-guardrails, detect-changes, build-and-deploy, probe-app]
    if: ${{ always() && needs.detect-changes.outputs.matrix != '[]' && needs.probe-app.result == 'success' && (matrix.environment == 'prod' || (github.event_name == 'workflow_dispatch' && inputs.enable_telemetry_gate == true)) && ((matrix.component == 'frontend' && vars.FRONTEND_RELEASE_ALARM_NAMES != '') || (matrix.component == 'backend' && vars.BACKEND_RELEASE_ALARM_NAMES != '')) }}
    strategy:
      fail-fast: false
      matrix:
        include: ${{ fromJson(needs.detect-changes.outputs.matrix) }}
    # Telemetry validation is intentionally a separate reusable workflow so smoke tests,
    # CloudWatch-based release checks, and rollback orchestration can evolve independently.
    uses: ./.github/workflows/reusable-evaluate-release-health.yml
    with:
      environment: ${{ matrix.environment }}
      component: ${{ matrix.component }}
      project_name: ${{ vars.PROJECT_NAME }}
      observation_window_minutes: ${{ (github.event_name == 'workflow_dispatch' && inputs.observation_window_minutes) || '5' }}
      alarm_names: ${{ (matrix.component == 'frontend' && vars.FRONTEND_RELEASE_ALARM_NAMES) || (matrix.component == 'backend' && vars.BACKEND_RELEASE_ALARM_NAMES) || '' }}
    secrets: inherit

  # ---------------------------------------------------------
  # 5. ROLLBACK (Use SSM last-known-good pointer when probe/finalize fails)
  # ---------------------------------------------------------
  rollback:
    name: Roll Back ${{ matrix.component }} (${{ matrix.environment }})
    needs: [predeploy-guardrails, detect-changes, build-and-deploy, probe-app, evaluate-release-health]
    if: ${{ always() && needs.detect-changes.outputs.matrix != '[]' && (needs.probe-app.result == 'failure' || needs.evaluate-release-health.result == 'failure') }}
    strategy:
      fail-fast: false
      matrix:
        include: ${{ fromJson(needs.detect-changes.outputs.matrix) }}
    # Automatic rollback is now a reusable workflow because the whole recovery sequence is
    # repeated orchestration: metadata download, pointer lookup, service rollback, and manifests.
    uses: ./.github/workflows/reusable-auto-rollback.yml
    with:
      environment: ${{ matrix.environment }}
      component: ${{ matrix.component }}
      project_name: ${{ vars.PROJECT_NAME }}
      service_launch_type: ${{ (github.event_name == 'workflow_dispatch' && inputs.default_launch_type) || '' }}
      service_network_mode: ${{ (github.event_name == 'workflow_dispatch' && inputs.default_network_mode) || '' }}
      # Rollback should target the same tier-specific network boundary the forward deploy used.
      security_group_name: ${{ (matrix.component == 'frontend' && vars.FRONTEND_SERVICE_SECURITY_GROUP_NAME) || (matrix.component == 'backend' && vars.BACKEND_SERVICE_SECURITY_GROUP_NAME) || vars.SERVICE_SECURITY_GROUP_NAME || '' }}
      subnet_tag_values: ${{ (matrix.component == 'frontend' && vars.FRONTEND_SERVICE_SUBNET_TAG_VALUES) || (matrix.component == 'backend' && vars.BACKEND_SERVICE_SUBNET_TAG_VALUES) || vars.SERVICE_SUBNET_TAG_VALUES || '' }}
      assign_public_ip: ${{ (matrix.component == 'frontend' && vars.FRONTEND_SERVICE_ASSIGN_PUBLIC_IP) || (matrix.component == 'backend' && vars.BACKEND_SERVICE_ASSIGN_PUBLIC_IP) || vars.SERVICE_ASSIGN_PUBLIC_IP || '' }}
    secrets: inherit

  # ---------------------------------------------------------
  # 5b. VERIFY AUTO ROLLBACK RECOVERY
  # ---------------------------------------------------------
  verify-rollback-recovery:
    name: Verify Rollback Recovery (${{ matrix.environment }})
    needs: [predeploy-guardrails, detect-changes, rollback]
    if: ${{ always() && needs.rollback.result == 'success' && needs.detect-changes.outputs.env_matrix != '[]' }}
    strategy:
      fail-fast: false
      matrix:
        environment: ${{ fromJson(needs.detect-changes.outputs.env_matrix) }}
    # Rollback should be treated like a recovery deployment, not assumed healthy by default.
    # We reuse the same probe workflow, but explicitly disable pointer advancement because
    # rollback verification is confirming recovery, not promoting a new revision.
    uses: ./.github/workflows/reusable-probe-environment.yml
    with:
      environment: ${{ matrix.environment }}
      project_name: ${{ vars.PROJECT_NAME }}
      # Rollback verification should only validate recovery using the already-published
      # probe image. Fetch-only keeps this path fast and avoids mutating probe artifacts.
      build_image: false
      fetch_image: true
      # Recovery verification should use the same fast probe runtime as normal validation.
      probe_launch_type: FARGATE
      probe_network_mode: awsvpc
      update_pointers_on_success: false
      probe_purpose: rollback_verification
    secrets: inherit

  # ---------------------------------------------------------
  # 5c. TELEMETRY GATE AFTER AUTO ROLLBACK VERIFICATION
  # ---------------------------------------------------------
  evaluate-rollback-health:
    name: Evaluate Rollback Health ${{ matrix.component }} (${{ matrix.environment }})
    needs: [predeploy-guardrails, detect-changes, rollback, verify-rollback-recovery]
    if: ${{ always() && needs.rollback.result == 'success' && needs.verify-rollback-recovery.result == 'success' && (matrix.environment == 'prod' || (github.event_name == 'workflow_dispatch' && inputs.enable_telemetry_gate == true)) && ((matrix.component == 'frontend' && vars.FRONTEND_RELEASE_ALARM_NAMES != '') || (matrix.component == 'backend' && vars.BACKEND_RELEASE_ALARM_NAMES != '')) }}
    strategy:
      fail-fast: false
      matrix:
        include: ${{ fromJson(needs.detect-changes.outputs.matrix) }}
    uses: ./.github/workflows/reusable-evaluate-release-health.yml
    with:
      environment: ${{ matrix.environment }}
      component: ${{ matrix.component }}
      project_name: ${{ vars.PROJECT_NAME }}
      observation_window_minutes: ${{ (github.event_name == 'workflow_dispatch' && inputs.observation_window_minutes) || '5' }}
      alarm_names: ${{ (matrix.component == 'frontend' && vars.FRONTEND_RELEASE_ALARM_NAMES) || (matrix.component == 'backend' && vars.BACKEND_RELEASE_ALARM_NAMES) || '' }}
      health_purpose: rollback_verification
    secrets: inherit

  # ---------------------------------------------------------
  # 6. MANUAL ROLLBACK (Workflow dispatch only)
  # ---------------------------------------------------------
  manual-rollback:
    name: Manual Roll Back ${{ inputs.rollback_component }} (${{ inputs.target_environment }})
    needs: predeploy-guardrails
    if: ${{ github.event_name == 'workflow_dispatch' && inputs.action_type == 'rollback' }}
    # Manual rollback remains a separate reusable workflow from auto rollback because its
    # contract is operator-driven: explicit ARN, manifest history, or trusted SSM fallback.
    uses: ./.github/workflows/reusable-manual-rollback.yml
    with:
      environment: ${{ inputs.target_environment }}
      component: ${{ inputs.rollback_component }}
      project_name: ${{ vars.PROJECT_NAME }}
      service_launch_type: ${{ (github.event_name == 'workflow_dispatch' && inputs.default_launch_type) || '' }}
      service_network_mode: ${{ (github.event_name == 'workflow_dispatch' && inputs.default_network_mode) || '' }}
      security_group_name: ${{ (inputs.rollback_component == 'frontend' && vars.FRONTEND_SERVICE_SECURITY_GROUP_NAME) || (inputs.rollback_component == 'backend' && vars.BACKEND_SERVICE_SECURITY_GROUP_NAME) || vars.SERVICE_SECURITY_GROUP_NAME || '' }}
      subnet_tag_values: ${{ (inputs.rollback_component == 'frontend' && vars.FRONTEND_SERVICE_SUBNET_TAG_VALUES) || (inputs.rollback_component == 'backend' && vars.BACKEND_SERVICE_SUBNET_TAG_VALUES) || vars.SERVICE_SUBNET_TAG_VALUES || '' }}
      assign_public_ip: ${{ (inputs.rollback_component == 'frontend' && vars.FRONTEND_SERVICE_ASSIGN_PUBLIC_IP) || (inputs.rollback_component == 'backend' && vars.BACKEND_SERVICE_ASSIGN_PUBLIC_IP) || vars.SERVICE_ASSIGN_PUBLIC_IP || '' }}
      rollback_task_definition_arn: ${{ inputs.rollback_task_definition_arn }}
      rollback_manifest_s3_key: ${{ inputs.rollback_manifest_s3_key }}
    secrets: inherit

  # ---------------------------------------------------------
  # 6b. VERIFY MANUAL ROLLBACK RECOVERY
  # ---------------------------------------------------------
  verify-manual-rollback-recovery:
    name: Verify Manual Rollback Recovery (${{ inputs.target_environment }})
    needs: [predeploy-guardrails, manual-rollback]
    if: ${{ always() && github.event_name == 'workflow_dispatch' && inputs.action_type == 'rollback' && needs.manual-rollback.result == 'success' }}
    # Manual rollback deserves the same health verification as automatic rollback.
    # We keep it separate from the rollback workflow itself so the graph clearly shows
    # "recovery action" and "recovery verification" as two different outcomes.
    uses: ./.github/workflows/reusable-probe-environment.yml
    with:
      environment: ${{ inputs.target_environment }}
      project_name: ${{ vars.PROJECT_NAME }}
      # Manual rollback recovery follows the same fetch-only probe rule as auto rollback.
      build_image: false
      fetch_image: true
      # Manual rollback verification should use the same fast probe runtime as normal validation.
      probe_launch_type: FARGATE
      probe_network_mode: awsvpc
      update_pointers_on_success: false
      probe_purpose: rollback_verification
    secrets: inherit

  # ---------------------------------------------------------
  # 6c. TELEMETRY GATE AFTER MANUAL ROLLBACK VERIFICATION
  # ---------------------------------------------------------
  evaluate-manual-rollback-health:
    name: Evaluate Manual Rollback Health (${{ inputs.target_environment }})
    needs: [predeploy-guardrails, manual-rollback, verify-manual-rollback-recovery]
    if: ${{ always() && needs.manual-rollback.result == 'success' && needs.verify-manual-rollback-recovery.result == 'success' && (inputs.target_environment == 'prod' || (github.event_name == 'workflow_dispatch' && inputs.enable_telemetry_gate == true)) && ((inputs.rollback_component == 'frontend' && vars.FRONTEND_RELEASE_ALARM_NAMES != '') || (inputs.rollback_component == 'backend' && vars.BACKEND_RELEASE_ALARM_NAMES != '')) }}
    uses: ./.github/workflows/reusable-evaluate-release-health.yml
    with:
      environment: ${{ inputs.target_environment }}
      component: ${{ inputs.rollback_component }}
      project_name: ${{ vars.PROJECT_NAME }}
      observation_window_minutes: ${{ inputs.observation_window_minutes }}
      alarm_names: ${{ (inputs.rollback_component == 'frontend' && vars.FRONTEND_RELEASE_ALARM_NAMES) || (inputs.rollback_component == 'backend' && vars.BACKEND_RELEASE_ALARM_NAMES) || '' }}
      health_purpose: rollback_verification
    secrets: inherit

  # ---------------------------------------------------------
  # 7. DB SEEDING (Manual Only)
  # ---------------------------------------------------------
  seed-database:
    name: Build & Run DB Seeder
    needs: [predeploy-guardrails, detect-changes]
    # ONLY runs on workflow dispatch AND if run_seeding is explicitly checked
    if: ${{ github.event_name == 'workflow_dispatch' && inputs.run_seeding == true }}
    strategy:
      fail-fast: false
      matrix:
        environment: ${{ fromJson(needs.detect-changes.outputs.env_matrix) }}
    # Database seeding is now reusable orchestration too: build/register the seeder revision,
    # execute it in the correct network mode, and surface the task result and logs consistently.
    uses: ./.github/workflows/reusable-seed-database.yml
    with:
      environment: ${{ matrix.environment }}
      project_name: ${{ vars.PROJECT_NAME }}
      # Seeder follows the same caller-owned image policy pattern as deploy and probe.
      # Today we intentionally build the seeder fresh for manual runs, but the choice is
      # visible here now and can be changed later without editing the reusable workflow.
      build_image: true
      fetch_image: false
      seeder_launch_type: ${{ (github.event_name == 'workflow_dispatch' && inputs.default_launch_type) || vars.SEEDER_LAUNCH_TYPE  }}
      seeder_network_mode: ${{ (github.event_name == 'workflow_dispatch' && inputs.default_network_mode) || vars.SEEDER_NETWORK_MODE }}
      # Seeder keeps its own networking contract because database mutation is a separate
      # execution concern from frontend/backend service rollout.
      seeder_security_group_name: ${{ vars.SEEDER_SECURITY_GROUP_NAME || '' }}
      seeder_subnet_tag_values: ${{ vars.SEEDER_SUBNET_TAG_VALUES || '' }}
      seeder_assign_public_ip: ${{ vars.SEEDER_ASSIGN_PUBLIC_IP || '' }}
    secrets: inherit

  notify-deploy-outcome:
    name: Notify Deploy Outcome ${{ matrix.component }} (${{ matrix.environment }})
    needs: [predeploy-guardrails, detect-changes, build-and-deploy, probe-app, evaluate-release-health, rollback, verify-rollback-recovery, evaluate-rollback-health]
    if: ${{ always() && needs.detect-changes.outputs.matrix != '[]' }}
    strategy:
      fail-fast: false
      matrix:
        include: ${{ fromJson(needs.detect-changes.outputs.matrix) }}
    runs-on: ubuntu-latest
    steps:
      - name: Resolve terminal deploy event
        id: status
        shell: bash
        run: |
          set -euo pipefail

          BUILD_RESULT="${{ needs.build-and-deploy.result }}"
          PROBE_RESULT="${{ needs.probe-app.result }}"
          TELEMETRY_RESULT="${{ needs.evaluate-release-health.result }}"
          ROLLBACK_RESULT="${{ needs.rollback.result }}"
          VERIFY_ROLLBACK_RESULT="${{ needs.verify-rollback-recovery.result }}"
          ROLLBACK_TELEMETRY_RESULT="${{ needs.evaluate-rollback-health.result }}"

          EVENT="deploy_failed"
          STATUS="failure"
          SUMMARY="Deployment did not reach a healthy terminal state."

          if [[ "$BUILD_RESULT" == "success" && "$PROBE_RESULT" == "success" && ( "$TELEMETRY_RESULT" == "success" || "$TELEMETRY_RESULT" == "skipped" ) ]]; then
            EVENT="deploy_succeeded"
            STATUS="success"
            SUMMARY="Deployment completed successfully and any enabled telemetry gate passed."
          elif [[ "$ROLLBACK_RESULT" == "success" && "$VERIFY_ROLLBACK_RESULT" == "success" && ( "$ROLLBACK_TELEMETRY_RESULT" == "success" || "$ROLLBACK_TELEMETRY_RESULT" == "skipped" ) ]]; then
            EVENT="rollback_recovered"
            STATUS="recovered"
            SUMMARY="Deployment failure triggered rollback, and rollback verification completed successfully."
          elif [[ "$ROLLBACK_RESULT" == "success" && ( "$VERIFY_ROLLBACK_RESULT" == "failure" || "$ROLLBACK_TELEMETRY_RESULT" == "failure" ) ]]; then
            EVENT="rollback_verification_failed"
            STATUS="failure"
            SUMMARY="Rollback executed, but post-rollback verification or rollback telemetry still reported the environment unhealthy."
          elif [[ "$ROLLBACK_RESULT" == "failure" ]]; then
            EVENT="rollback_failed"
            STATUS="failure"
            SUMMARY="Rollback was triggered but did not complete successfully."
          elif [[ "$PROBE_RESULT" == "failure" || "$TELEMETRY_RESULT" == "failure" ]]; then
            EVENT="rollback_triggered"
            STATUS="degraded"
            SUMMARY="Deployment validation failed and rollback handling was triggered."
          fi

          echo "event=$EVENT" >> "$GITHUB_OUTPUT"
          echo "status=$STATUS" >> "$GITHUB_OUTPUT"
          echo "summary=$SUMMARY" >> "$GITHUB_OUTPUT"

      - name: Configure AWS credentials for notification publishing
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::${{ vars.ACCOUNT_ID }}:role/GitHubActions-ECS-DeployRole
          aws-region: ${{ matrix.environment == 'prod' && (vars.PROD_AWS_REGION || 'us-east-2') || (vars.DEV_AWS_REGION || 'us-east-1') }}

      - name: Publish terminal deploy notification
        uses: ./.github/actions/send-release-notification
        with:
          topic_arn: ${{ matrix.environment == 'prod' && vars.PROD_RELEASE_NOTIFICATIONS_TOPIC_ARN || vars.DEV_RELEASE_NOTIFICATIONS_TOPIC_ARN || '' }}
          aws_region: ${{ matrix.environment == 'prod' && (vars.PROD_AWS_REGION || 'us-east-2') || (vars.DEV_AWS_REGION || 'us-east-1') }}
          notification_event: ${{ steps.status.outputs.event }}
          project_name: ${{ vars.PROJECT_NAME }}
          environment: ${{ matrix.environment }}
          component: ${{ matrix.component }}
          status: ${{ steps.status.outputs.status }}
          release_version: ${{ needs.predeploy-guardrails.outputs.resolved_release_version }}
          change_ticket: ${{ github.event_name == 'workflow_dispatch' && inputs.change_ticket || '' }}
          workflow_url: ${{ format('{0}/{1}/actions/runs/{2}', github.server_url, github.repository, github.run_id) }}
          summary: ${{ steps.status.outputs.summary }}

  notify-manual-rollback-outcome:
    name: Notify Manual Rollback Outcome (${{ inputs.target_environment }})
    needs: [predeploy-guardrails, manual-rollback, verify-manual-rollback-recovery, evaluate-manual-rollback-health]
    if: ${{ always() && github.event_name == 'workflow_dispatch' && inputs.action_type == 'rollback' }}
    runs-on: ubuntu-latest
    steps:
      - name: Resolve terminal manual rollback event
        id: status
        shell: bash
        run: |
          set -euo pipefail

          ROLLBACK_RESULT="${{ needs.manual-rollback.result }}"
          VERIFY_RESULT="${{ needs.verify-manual-rollback-recovery.result }}"
          TELEMETRY_RESULT="${{ needs.evaluate-manual-rollback-health.result }}"

          EVENT="manual_rollback_failed"
          STATUS="failure"
          SUMMARY="Manual rollback did not complete successfully."

          if [[ "$ROLLBACK_RESULT" == "success" && "$VERIFY_RESULT" == "success" && ( "$TELEMETRY_RESULT" == "success" || "$TELEMETRY_RESULT" == "skipped" ) ]]; then
            EVENT="manual_rollback_succeeded"
            STATUS="success"
            SUMMARY="Manual rollback completed successfully and post-rollback validation reported the environment healthy."
          elif [[ "$ROLLBACK_RESULT" == "success" && ( "$VERIFY_RESULT" == "failure" || "$TELEMETRY_RESULT" == "failure" ) ]]; then
            EVENT="manual_rollback_verification_failed"
            STATUS="failure"
            SUMMARY="Manual rollback executed, but rollback verification or rollback telemetry still reported the environment unhealthy."
          fi

          echo "event=$EVENT" >> "$GITHUB_OUTPUT"
          echo "status=$STATUS" >> "$GITHUB_OUTPUT"
          echo "summary=$SUMMARY" >> "$GITHUB_OUTPUT"

      - name: Configure AWS credentials for notification publishing
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::${{ vars.ACCOUNT_ID }}:role/GitHubActions-ECS-DeployRole
          aws-region: ${{ inputs.target_environment == 'prod' && (vars.PROD_AWS_REGION || 'us-east-2') || (vars.DEV_AWS_REGION || 'us-east-1') }}

      - name: Publish manual rollback notification
        uses: ./.github/actions/send-release-notification
        with:
          topic_arn: ${{ inputs.target_environment == 'prod' && vars.PROD_RELEASE_NOTIFICATIONS_TOPIC_ARN || vars.DEV_RELEASE_NOTIFICATIONS_TOPIC_ARN || '' }}
          aws_region: ${{ inputs.target_environment == 'prod' && (vars.PROD_AWS_REGION || 'us-east-2') || (vars.DEV_AWS_REGION || 'us-east-1') }}
          notification_event: ${{ steps.status.outputs.event }}
          project_name: ${{ vars.PROJECT_NAME }}
          environment: ${{ inputs.target_environment }}
          component: ${{ inputs.rollback_component }}
          status: ${{ steps.status.outputs.status }}
          release_version: ${{ needs.predeploy-guardrails.outputs.resolved_release_version }}
          change_ticket: ${{ inputs.change_ticket || '' }}
          workflow_url: ${{ format('{0}/{1}/actions/runs/{2}', github.server_url, github.repository, github.run_id) }}
          summary: ${{ steps.status.outputs.summary }}
